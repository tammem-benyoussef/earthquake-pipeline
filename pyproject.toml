[project]
name = "earthquake-pipeline"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "tammem", email = "tammembenyoussef2020@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
earthquake-pipeline = "earthquake_pipeline:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
