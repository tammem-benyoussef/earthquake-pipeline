name: 'dbt_earthquakes'
version: '1.0.0'
profile: 'earthquake_pipeline'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  dbt_earthquakes:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts